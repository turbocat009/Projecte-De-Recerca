<a href={Astro.props.href} class="card">
    <h1>{Astro.props.title}</h1>
    <slot />
</a>

<style>
    a.card {
        display: flex;
        flex-direction: column;
        align-items: start;
        justify-content: start;
        width: 100%;
        max-width: 300px;
        height: 200px;
        padding: 2rem 1rem;
        background-color: #FDFDFD;
        border-radius: 10px;
        box-shadow: 0px 8px 6px rgba(0, 0, 0, 0.2);
        text-decoration: none;
        color: black;
        transition: transform 0.3s ease, box-shadow 0.3s ease;
        cursor: pointer;
    }

    .card:hover {
        transform: translateY(-5px);
        box-shadow: 0 12px 10px rgba(0, 0, 0, 0.3);
    }
    
    .card h1 {
        font-size: 2rem;
        font-weight: 500;
        margin: 0;
        text-align: start;
    }

    .card p {
        font-size: 1rem;
        color: #555;
        margin-top: 1rem;
    }
</style>